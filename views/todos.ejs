<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tekser To-Do List</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50 min-h-screen font-sans">
    <%- include('partials/navbar') %>

    <div class="max-w-2xl mx-auto p-6 mt-6">
      <div class="text-center mb-8">
        <h1 class="text-3xl font-extrabold text-gray-900 tracking-tight">
          To-Do Dashboard
        </h1>
        <p class="text-gray-500 mt-2">Manage your tasks efficiently.</p>
      </div>

      <% if (error) { %>
      <div
        class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded shadow-sm"
        role="alert"
      >
        <p class="font-bold">Error</p>
        <p><%= error %></p>
      </div>
      <% } %>

      <div
        class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 mb-8"
      >
        <form action="/add" method="POST" class="flex gap-3">
          <input
            type="text"
            name="task"
            placeholder="New task details..."
            class="flex-1 border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition"
            required
          />
          <button
            type="submit"
            class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 px-6 rounded-lg shadow-md transition transform hover:-translate-y-0.5"
          >
            <i class="fas fa-plus"></i>
          </button>
        </form>
      </div>

      <div class="space-y-4">
        <% todos.forEach(todo => { %>
        <div
          class="group flex items-center justify-between p-5 bg-white rounded-xl shadow-sm border border-gray-100 hover:shadow-md transition duration-200"
        >
          <div class="flex items-center gap-4 flex-1">
            <form action="/toggle/<%= todo.id %>" method="POST" class="m-0">
              <button
                type="submit"
                class="text-2xl focus:outline-none transition transform active:scale-95 <%= todo.completed ? 'text-green-500' : 'text-gray-300 hover:text-green-500' %>"
              >
                <i
                  class="<%= todo.completed ? 'fas fa-check-circle' : 'far fa-circle' %>"
                ></i>
              </button>
            </form>

            <div class="flex flex-col">
              <span
                class="text-lg font-medium <%= todo.completed ? 'line-through text-gray-400' : 'text-gray-800' %>"
              >
                <%= todo.task %>
              </span>
              <span class="text-xs text-gray-400">
                <%= new Date(todo.created_at).toLocaleString('id-ID') %>
              </span>
            </div>
          </div>

          <form
            action="/delete/<%= todo.id %>"
            method="POST"
            class="m-0 opacity-0 group-hover:opacity-100 transition-opacity duration-200"
            onsubmit="return confirm('Delete this task?');"
          >
            <button
              type="submit"
              class="text-gray-400 hover:text-red-500 p-2 transition"
            >
              <i class="fas fa-trash-alt"></i>
            </button>
          </form>
        </div>
        <% }) %> <% if (todos.length === 0) { %>
        <div class="text-center py-12">
          <div
            class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-blue-100 text-blue-500 mb-4"
          >
            <i class="fas fa-clipboard-check text-2xl"></i>
          </div>
          <h3 class="text-lg font-medium text-gray-900">No tasks yet</h3>
          <p class="text-gray-500">Get started by creating a new task above.</p>
        </div>
        <% } %>
      </div>

      <footer class="mt-12 text-center text-sm text-gray-400 border-t pt-6">
        &copy; 2024 Praktikum Teknologi Server
      </footer>
    </div>
  </body>
</html>
